---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  labels:
    cluster.x-k8s.io/v1beta2: v1alpha1
  name: stackitclusters.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: StackitCluster
    listKind: StackitClusterList
    plural: stackitclusters
    singular: stackitcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.initialization.provisioned
      name: Provisioned
      type: boolean
    - jsonPath: .status.networkID
      name: Network
      type: string
    - jsonPath: .spec.controlPlaneEndpoint.host
      name: Endpoint
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StackitCluster is the Schema for the stackitclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StackitCluster
            properties:
              controlPlaneEndpoint:
                description: |-
                  controlPlaneEndpoint represents the endpoint used to communicate with the control plane.
                  It is set by the StackitCluster controller once the control-plane public IP is reserved,
                  unless already set here to bring your own endpoint (e.g. an externally managed load balancer).
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              identityRef:
                description: |-
                  identityRef is a reference to a StackitClusterIdentity in the same namespace,
                  used to authenticate against the STACKIT API. If nil, the manager's ambient
                  STACKIT credentials (env vars / default credentials file) are used.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              network:
                description: network configures the network used by this cluster's
                  machines.
                properties:
                  id:
                    description: |-
                      id is the ID of an existing STACKIT network to use for this cluster.
                      If empty, the controller creates a new isolated network named "<cluster-name>-network".
                    type: string
                type: object
              projectId:
                description: projectId is the STACKIT project ID that owns all resources
                  created for this cluster.
                minLength: 1
                type: string
              region:
                description: region is the STACKIT region (e.g. "eu01") to create
                  resources in.
                minLength: 1
                type: string
            required:
            - projectId
            - region
            type: object
          status:
            description: status defines the observed state of StackitCluster
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the StackitCluster resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Ready": the resource is fully functional

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              controlPlanePublicIPID:
                description: controlPlanePublicIPID is the ID of the public IP reserved
                  for the control-plane endpoint.
                type: string
              failureDomains:
                description: |-
                  failureDomains is a list of failure domain objects synced from the STACKIT availability
                  zones available in spec.region.
                items:
                  description: |-
                    FailureDomain is the Schema for Cluster API failure domains.
                    It allows controllers to understand how many failure domains a cluster can optionally span across.
                  properties:
                    attributes:
                      additionalProperties:
                        type: string
                      description: attributes is a free form map of attributes an
                        infrastructure provider might use or require.
                      type: object
                    controlPlane:
                      description: controlPlane determines if this failure domain
                        is suitable for use by control plane machines.
                      type: boolean
                    name:
                      description: name is the name of the failure domain.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              initialization:
                description: initialization provides observations of the StackitCluster
                  provisioning process.
                properties:
                  provisioned:
                    description: |-
                      provisioned is true when the infrastructure backing the cluster (network, security group,
                      control-plane public IP) has been fully created.
                    type: boolean
                type: object
              networkID:
                description: networkID is the ID of the network backing this cluster.
                type: string
              networkManaged:
                description: |-
                  networkManaged is true if the network referenced by networkID was created by this
                  controller (and should be deleted when the cluster is deleted), false if it was
                  brought by the user via spec.network.id.
                type: boolean
              securityGroupID:
                description: securityGroupID is the ID of the security group applied
                  to all machines in this cluster.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
