---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  labels:
    cluster.x-k8s.io/v1beta2: v1alpha1
  name: stackitmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: StackitMachine
    listKind: StackitMachineList
    plural: stackitmachines
    singular: stackitmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.initialization.provisioned
      name: Provisioned
      type: boolean
    - jsonPath: .status.instanceState
      name: InstanceState
      type: string
    - jsonPath: .spec.providerID
      name: ProviderID
      priority: 10
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StackitMachine is the Schema for the stackitmachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StackitMachine
            properties:
              availabilityZone:
                description: |-
                  availabilityZone pins the server to a specific STACKIT availability zone. If unset, the
                  value is taken from the owning Machine's spec.failureDomain.
                type: string
              imageId:
                description: imageId is the ID of the STACKIT image (e.g. an Ubuntu
                  image) used as the boot volume source.
                minLength: 1
                type: string
              machineType:
                description: machineType is the STACKIT machine type (flavor) to use,
                  e.g. "g1.2".
                minLength: 1
                type: string
              providerID:
                description: |-
                  providerID is the identification ID of the server as understood by the STACKIT provider.
                  It is set by the controller once the server is created, in the form
                  "stackit://<projectId>/<region>/<serverId>".
                type: string
              rootDiskSizeGB:
                description: rootDiskSizeGB is the size in GB of the boot volume.
                  Defaults to 32 if unset.
                format: int64
                minimum: 1
                type: integer
              sshKeyName:
                description: sshKeyName is the name of an existing STACKIT SSH key
                  pair to inject into the server.
                type: string
            required:
            - imageId
            - machineType
            type: object
          status:
            description: status defines the observed state of StackitMachine
            properties:
              addresses:
                description: addresses contains the associated addresses for the backing
                  STACKIT server.
                items:
                  description: MachineAddress contains information for the node's
                    address.
                  properties:
                    address:
                      description: address is the machine address.
                      maxLength: 256
                      minLength: 1
                      type: string
                    type:
                      description: type is the machine address type, one of Hostname,
                        ExternalIP, InternalIP, ExternalDNS or InternalDNS.
                      enum:
                      - Hostname
                      - ExternalIP
                      - InternalIP
                      - ExternalDNS
                      - InternalDNS
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              conditions:
                description: |-
                  conditions represent the current state of the StackitMachine resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Ready": the resource is fully functional

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failureDomain:
                description: failureDomain is the failure domain (availability zone)
                  the server was actually created in.
                type: string
              failureMessage:
                description: failureMessage will be set in the event that there is
                  a terminal problem reconciling the machine.
                type: string
              failureReason:
                description: failureReason will be set in the event that there is
                  a terminal problem reconciling the machine.
                type: string
              initialization:
                description: initialization provides observations of the StackitMachine
                  provisioning process.
                properties:
                  provisioned:
                    description: provisioned is true when the backing STACKIT server
                      has been created and is running.
                    type: boolean
                type: object
              instanceState:
                description: instanceState is the last observed STACKIT server status
                  (e.g. "CREATING", "ACTIVE", "ERROR").
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
